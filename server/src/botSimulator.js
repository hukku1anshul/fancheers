// Bot simulator permanently removed as per user instruction:
// "please remove any simulated data, the app should have only real data, everything should be wired on without any simulations"
export function startBotSimulator() {
  // No-op. All simulated bots and fake cheer generators are disabled.
}
